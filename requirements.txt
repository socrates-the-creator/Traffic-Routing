setuptools>=65.5.0
wheel

# Core web framework
flask==2.2.5
gunicorn==21.2.0
python-dotenv==1.0.0

# Data processing and analysis (force wheels)
numpy==1.24.3 --only-binary=:all:
pandas==2.0.3 --only-binary=:all:
scikit-learn==1.3.0 --only-binary=:all:

# Geographic data processing (force wheels)
geopandas==0.13.2 --only-binary=:all:
shapely==2.0.1 --only-binary=:all:
networkx==3.1
osmnx==1.6.0
fiona==1.9.4 --only-binary=:all:
pyproj==3.6.0 --only-binary=:all:
rtree==1.0.1 --only-binary=:all:

# Machine learning
tensorflow==2.14.0 --only-binary=:all:
protobuf==3.20.3

# Visualization
matplotlib==3.7.2 --only-binary=:all:
seaborn==0.12.2
folium==0.14.0
plotly==5.15.0

# Web interface
dash==2.11.1
dash-bootstrap-components==1.4.1

# API and data fetching
requests==2.31.0
overpy==0.6

# Additional utilities
schedule==1.2.0

# Fix for dependency conflicts
typing-extensions==4.12.2
